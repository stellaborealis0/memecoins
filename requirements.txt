# ============================================================
# Memecoin Agent v3.0-ultralite-fixed - Requirements.txt
# Optimizado para MacBook Pro 7,1 (8GB RAM) + IM (fallback)
# SAA v7.2 compliant - 100% SQLite, sin Docker, sin gRPC
# Python 3.11 requerido (NO usar 3.12 - solana-py incompatible)
# ============================================================

# --- Python y versiones exactas ---
# Python 3.11 requerido. NO usar 3.12 (solana-py incompatible).
# Verificar: python --version

# --- Core (sin gRPC, sin Docker) ---
solana==0.34.0
solders==0.21.0
requests>=2.31
websockets>=12.0

# --- LLM vía LiteLLM Gateway (TO:8080) ---
openai>=1.30

# --- ML (CPU-only, sin torch) ---
xgboost>=2.0
scikit-learn>=1.4
pandas>=2.0
numpy>=1.26

# --- Scheduler ---
apscheduler>=3.10

# --- Telegram ---
python-telegram-bot>=20.0

# --- Retry ---
tenacity>=8.2

# --- Utilidades ---
python-dotenv>=1.0.1
pydantic>=2.7.1
structlog>=24.1.0