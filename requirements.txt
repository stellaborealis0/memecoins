# ============================================================
# Memecoin Agent v3.0-ultralite - Requirements.txt
# Optimizado para MacBook Pro 7,1 (8GB RAM) + IM (fallback)
# SAA v7.2 compliant - 100% SQLite, sin Docker, sin gRPC
# ============================================================

# --- Core (sin gRPC, sin Docker) ---
solana>=0.34
solders>=0.21
requests>=2.31
websockets>=12.0

# --- ML (CPU-only, sin torch) ---
xgboost>=2.0
scikit-learn>=1.4
pandas>=2.0
numpy>=1.26

# --- Scheduler (solo uno) ---
apscheduler>=3.10

# --- Telegram ---
python-telegram-bot>=20.0

# --- Retry ---
tenacity>=8.2

# --- Utilidades ---
python-dotenv>=1.0.1
pydantic>=2.7.1
structlog>=24.1.0

# --- LLM vía LiteLLM Gateway (TO:8080) ---
# NO openai - usar requests directo a http://100.68.1.180:8080/v1